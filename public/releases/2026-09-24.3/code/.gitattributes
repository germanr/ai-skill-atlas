# Archived bytes are covered by SHA-256 manifests. Never convert their line endings.
public/releases/** -text

# The social-card receipt hashes this template exactly on every platform.
scripts/social-card.html text eol=lf
